node_modules/
build/
dist/
coverage/
docs/
.DS_Store
*.log
.env*
!.env.example
diff*.txt
extracted_app.txt
ChickenGod POV Novice vs. Mungo (Game 2).mp4
diff_app.txt
gif_diff_app.txt
EXAMPLE FILES/

# Live Tuning dev parity (local-only mirror of the D1 official preset)
data/multiplayer-config.json
# Cloudflare Worker local secrets
worker/.dev.vars

# RL trainer artifacts (TensorBoard runs, venv, checkpoints) — see python/
/runs/
python/.venv/

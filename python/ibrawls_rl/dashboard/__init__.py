"""Local web control board for the iBrawls RL trainer.

Run it with::

    python -m ibrawls_rl.dashboard

then open the URL it prints. It edits the config, starts/stops training, streams
live charts, browses past runs, and grades saved models — so you never have to
hand-edit TOML or read TensorBoard.
"""

name = "ibrawlswebapp"
main = "src/index.ts"
compatibility_date = "2024-03-20"

# Bindings to Durable Objects
[durable_objects]
bindings = [
  { name = "GAME_LOBBY", class_name = "GameLobby" }
]

# Durable Object migrations (required for instantiation on the Free plan)
[[migrations]]
tag = "v1"
new_sqlite_classes = ["GameLobby"]
